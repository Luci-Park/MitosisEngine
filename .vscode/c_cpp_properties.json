{
    "version": 4,
    "configurations": [
        {
            "name": "CMake",
            // Flags (incl. -std:c++20 and include paths) come from cmake-tools,
            // so this never drifts from CMakeLists.txt.
            "configurationProvider": "ms-vscode.cmake-tools"
        }
    ]
}
